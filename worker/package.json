{
  "name": "berrova-checkout-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that creates Stripe Checkout Sessions for the Berrova store.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "vitest": "^2.1.9",
    "wrangler": "^3.86.0"
  }
}
